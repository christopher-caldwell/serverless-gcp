{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "invoke:test": "serverless invoke local --function Test --verbose",
    "invoke:something-else": "serverless invoke local --function SomethingElse --verbose"
  },
  "dependencies": {
    "serverless": "^3.24.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "serverless-esbuild": "^1.33.1",
    "ts-node": "^10.9.1"
  }
}
